<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Wxapp.mapper.ProcesserOrderMapper">

    <insert id="insertProcesserOrder" parameterType="ProcesserOrder">
        insert into ProcesserOrder values(#{OpenId},#{OrderId},#{PAddress},#{Phone},#{Kind},
        #{ServiceType},#{ReleaseTime},#{EndTime},#{Describe},#{Lon},#{Lat},#{Handler},#{GiveMark},#{Appraise},#{State})
    </insert>




</mapper>